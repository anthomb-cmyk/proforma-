[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "npm --prefix proforma-web install"
]

[phases.build]
cmds = [
  "CI=false npm --prefix proforma-web run build || echo 'Frontend build failed, API-only mode'"
]

[start]
cmd = "node server.js"
